/**
 * Client-safe config only — no secrets.
 * Therapist Railway host (not store production).
 */
export const PAYUP_RAILWAY_BASE_URL = "https://payup-therapists-production.up.railway.app";

export const PAYUP_TOKEN_REFRESH_SKEW_MS = 60_000;
